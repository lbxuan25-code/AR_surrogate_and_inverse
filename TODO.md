# TODO

## Current Task

### Task 2 — Define Dataset Schema And Sampling Policy

#### Goal
Define the canonical dataset row schema and initial sampling policy for
fit-layer controls and transport nuisance controls.

#### Deliverables
- dataset schema note;
- sampling policy note;
- small deterministic smoke dataset.

#### Acceptance
Dataset rows must include forward request, forward output reference, split
label, sampling policy id, and full forward-interface metadata.

---

## Backlog

### Task 3 — Implement Dataset Generation Orchestration

#### Goal
Generate datasets by calling the external forward repository through the stable
interface.

#### Deliverables
- dataset generation CLI;
- resumable manifest format;
- smoke dataset under `outputs/datasets/`;
- tests for metadata completeness.

#### Acceptance
No normal-state, pairing, projection, interface, or BTK code is duplicated.

---

### Task 4 — Train First Surrogate Baseline

#### Goal
Train the first lightweight surrogate from fit-layer + transport controls to AR
spectra.

#### Deliverables
- training script;
- model definition;
- checkpoint output;
- training and validation metrics;
- model card with dataset version metadata.

#### Acceptance
The surrogate must be evaluated against held-out forward spectra generated by
the external forward interface.

---

### Task 5 — Surrogate Evaluation And Calibration

#### Goal
Quantify where the surrogate is accurate enough for inverse search and where it
must fall back to direct forward calls.

#### Deliverables
- evaluation metrics;
- calibration diagnostics;
- error-by-transport-regime report;
- fallback policy.

#### Acceptance
The report must identify regimes where surrogate error is unsafe for inverse
claims.

---

### Task 6 — Inverse Search Prototype

#### Goal
Build the first inverse-search workflow that returns candidate families rather
than a unique microscopic truth.

#### Deliverables
- inverse search CLI;
- candidate-family output schema;
- final direct-forward recheck step;
- smoke inverse report.

#### Acceptance
Every reported candidate family must include pairing controls, transport
nuisance controls, uncertainty ranges, objective score, and forward recheck
metadata.

---

### Task 7 — Experiment-Fitting Report Workflow

#### Goal
Fit experimental AR spectra while keeping preprocessing, transport nuisance,
and order-parameter feature claims separated.

#### Deliverables
- experiment data ingest schema;
- preprocessing metadata schema;
- report template;
- one synthetic experiment smoke test.

#### Acceptance
Reports must not claim a unique microscopic RMFT point.

---

## Archive

### Task 1 — Bootstrap Repository Skeleton And Forward Dependency

Completed 2026-04-21.

#### Verification
- `pytest` passed: 5 tests.
- Generated payload: `outputs/datasets/fit_layer_forward_smoke_payload.json`.
- Run metadata: `outputs/runs/task1_forward_smoke_run_metadata.json`.
- Forward metadata recorded: `forward_interface_version`,
  `output_schema_version`, `pairing_convention_id`,
  `formal_baseline_record`, `formal_baseline_selection_rule`,
  `projection_config`, `git_commit`, and `git_dirty`.
