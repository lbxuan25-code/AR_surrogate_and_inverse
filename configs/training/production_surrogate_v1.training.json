{
  "config_role": "production_surrogate_v1_training_contract",
  "execution_mode": "local_production_preparation_contract",
  "model_type": "neural_residual_mlp_spectrum_surrogate",
  "dataset_manifest": "outputs/datasets/production_surrogate_v1/dataset.json",
  "checkpoint_dir": "outputs/checkpoints/production_surrogate_v1",
  "run_metadata_path": "outputs/runs/production_surrogate_v1/training_run_metadata.json",
  "observability_dir": "outputs/runs/production_surrogate_v1/training_observability",
  "run_kind": "production_surrogate_v1_training",
  "model_card_title": "Production Surrogate V1 Residual Checkpoint",
  "model_card_purpose_lines": [
    "First user-approved production-scale rectified residual surrogate run.",
    "This run uses the frozen S1-S8 representation, sampling, observability, and model-capacity decisions."
  ],
  "model_card_limitations_lines": [
    "Prepared by P1 and executed only in P2 after user acceptance.",
    "The architecture is intentionally unchanged under the S7 decision rule.",
    "Active learning is deferred until after production v1 review."
  ],
  "residual_hidden_width": 384,
  "residual_num_blocks": 5,
  "normalization": "layernorm",
  "activation": "gelu",
  "optimizer": "adamw",
  "learning_rate": 0.0004,
  "weight_decay": 0.00005,
  "batch_size": 48,
  "max_epochs": 450,
  "early_stopping_patience": 40,
  "random_seed": 1103,
  "device": "auto",
  "require_cuda": true,
  "held_out_splits": [
    "validation",
    "test"
  ],
  "loss": {
    "kind": "weighted_mse_plus_first_difference",
    "reconstruction_weight": 1.0,
    "shape_weight": 0.15,
    "bias_weighting": {
      "kind": "center_window",
      "central_weight": 2.5,
      "outer_weight": 1.0,
      "central_bias_half_width_mev": 6.0
    }
  },
  "ensemble": {
    "enabled": false,
    "policy": "single_member_first_production_run",
    "seed": 1103,
    "later_review_option": "P3 may request additional seeds only after reviewing production v1 observability."
  },
  "observability_standard_reference": "docs/standards/training_observability_standard.md",
  "model_capacity_decision_reference": "docs/model_capacity_decision_rules.md",
  "active_learning_reference": {
    "source_document": "docs/active_learning_plan.md",
    "status": "deferred_not_part_of_production_v1"
  },
  "supersedes": {
    "codex_proposed_s9_s10": "superseded_by_user_approved_p1"
  }
}
