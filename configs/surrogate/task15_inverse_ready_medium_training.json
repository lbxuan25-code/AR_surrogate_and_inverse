{
  "config_role": "current_task15_inverse_ready_medium_training_contract",
  "execution_mode": "local_preparation_inverse_ready_medium_contract",
  "model_type": "neural_residual_mlp_spectrum_surrogate",
  "dataset_manifest": "outputs/datasets/task15_inverse_ready_medium/dataset.json",
  "checkpoint_dir": "outputs/checkpoints/task15_inverse_ready_medium",
  "run_metadata_path": "outputs/runs/task15_inverse_ready_medium_training_run_metadata.json",
  "run_kind": "task15_inverse_ready_medium_training",
  "model_card_title": "Task 15 Inverse-Ready Medium Contract Checkpoint",
  "model_card_purpose_lines": [
    "Inverse-ready medium-scale residual surrogate mapping projected 7+1 pairing structure, truth-grade direction semantics, expanded nuisance controls, and low-dimensional TB pilot coordinates to full AR conductance spectra.",
    "This contract combines the canonical Task 14 pairing, bias40, nuisance-domain, and TB pilot rules into one medium-scale server handoff target."
  ],
  "model_card_limitations_lines": [
    "This Task 15 checkpoint must only be generated later on the server from the frozen inverse-ready medium dataset contract.",
    "The contract remains limited to supported named in-plane modes and narrow named-mode-centered spread only.",
    "Final deployment decisions still depend on held-out review against the committed ridge, Task 12B, and Task 13B baselines."
  ],
  "row_budget": {
    "total_rows": 9600,
    "train_rows": 7680,
    "validation_rows": 960,
    "test_rows": 960
  },
  "pairing_representation_contract": {
    "pairing_representation_version": "projected_7plus1_gauge_fixed_v1",
    "storage_block": "controls.pairing_representation",
    "global_phase_only": true,
    "further_compression": "forbidden"
  },
  "tb_pilot_contract": {
    "tb_coordinate_system_version": "task14_tb_pilot_latent_v1",
    "storage_block": "controls.tb_pilot_coordinates",
    "allowed_basic_guards": [
      "solver_stability",
      "finite_forward_output",
      "schema_validity"
    ]
  },
  "output_spectrum_contract": {
    "bias_min_mev": -40.0,
    "bias_max_mev": 40.0,
    "num_bias": 241
  },
  "residual_hidden_width": 384,
  "residual_num_blocks": 5,
  "normalization": "layernorm",
  "activation": "gelu",
  "optimizer": "adamw",
  "learning_rate": 0.0004,
  "weight_decay": 0.00005,
  "batch_size": 48,
  "max_epochs": 450,
  "early_stopping_patience": 40,
  "random_seed": 1103,
  "device": "auto",
  "held_out_splits": [
    "validation",
    "test"
  ],
  "loss": {
    "kind": "weighted_mse_plus_first_difference",
    "reconstruction_weight": 1.0,
    "shape_weight": 0.15,
    "bias_weighting": {
      "kind": "center_window",
      "central_weight": 2.5,
      "outer_weight": 1.0,
      "central_bias_half_width_mev": 6.0
    }
  },
  "ensemble": {
    "enabled": true,
    "seeds": [
      1103,
      2207,
      3301
    ],
    "aggregation": "mean",
    "disagreement_summary": "per_spectrum_mean_std_and_max_std"
  },
  "frozen_direction_contract": {
    "direction_schema_version": "ar_inverse_direction_v1",
    "supported_truth_grade_modes": [
      "inplane_100",
      "inplane_110"
    ],
    "unsupported_modes": [
      "c_axis"
    ],
    "diagnostic_raw_angles": "excluded from the canonical inverse-ready medium contract"
  }
}
